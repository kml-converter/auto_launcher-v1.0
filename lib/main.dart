import 'package:flutter/material.dart';
import 'schermata_principale.dart';

void main() => runApp(const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: SchermataPrincipale(), // Parte dall'assistente vocale
    ));
